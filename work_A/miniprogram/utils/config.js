// utils/config.js
// 统一配置后端地址, 需要改 IP/端口时只改这里

const config = {
  // 开发者工具 + 本机 Flask: 可用 http://127.0.0.1:5000
  // 真机预览 / 体验版：127.0.0.1 指向手机自己，必然连不上电脑，须改为电脑局域网 IP，例如 http://192.168.1.5:5000
  // 若提示「域名校验失败」：开发者工具 → 详情 → 本地设置 → 勾选「不校验合法域名…」；正式版需在小程序后台配置 request 合法域名（须 https）
  baseUrl: "http://127.0.0.1:5000",
};

module.exports = config;
